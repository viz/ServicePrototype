<h1>Editing Organisations</h1>

<% form_for(@organisation) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @organisation %> |
<%= link_to 'Back', organisations_path %>